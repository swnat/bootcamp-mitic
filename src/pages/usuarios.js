import React from 'react';
import PropTypes from 'prop-types';

const Usuarios = props => {
    return (
        <div>
            <h1>Usuarios</h1>
        </div>
    );
};


export default Usuarios;